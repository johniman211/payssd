# Environment variables for Payssd (Vite)
# Copy this file to `.env` and fill in real values

# Supabase project URL

# Supabase anon public key


# Optional: email notifications via Resend (used by Edge Function notify)
# Set these in Supabase Functions secrets, not in the client .env, for production.
# Listed here only for local testing of function calls.
RESEND_API_KEY="re_B11mqba5_3z6W4dXLiKS9CXFMhabmw1GT"
NOTIFY_FROM_EMAIL="notifications@payssd.com"

# Notes:
# - After creating `.env`, restart the dev server.
# - Edge Function notify also needs server-side secrets set via Supabase CLI:
#   supabase functions secrets set SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=...
- 
Secret key

